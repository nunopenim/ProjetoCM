<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulusofona.deisi.a2020.cm.g3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoCM">
        <activity android:name=".ListaTestesActivity" android:label="@string/lista_testes"/>
        <activity android:name=".VacinacaoActivity" android:label="@string/vacinas"/>
        <activity android:name=".ContactosActivity" android:label="@string/contactos"/>
        <activity android:name=".MainActivity" android:label="@string/Dashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>