<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulusofona.deisi.a2020.cm.g3">
    
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoCM">
        
        <provider
            android:authorities="pt.ulusofona.deisi.a2020.cm.g3.fileprovider"
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/fileprovider"/>

        </provider>
        
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity
            android:name=".PhotoViewActivity"
            android:label="@string/test_foto" />
        <activity
            android:name=".TesteDetailActivity"
            android:label="@string/teste_detail" />
        <activity
            android:name=".RegistoTesteActivity"
            android:label="@string/add_test" />
        <activity
            android:name=".ListaTestesActivity"
            android:label="@string/lista_testes" />
        <activity
            android:name=".VacinacaoActivity"
            android:label="@string/vacinas" />
        <activity
            android:name=".ContactosActivity"
            android:label="@string/contactos" /> -->
        <activity
            android:name=".MainActivity"
            android:label="@string/Dashboard"></activity>
    </application>

</manifest>